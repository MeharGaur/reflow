{
  "name": "reflow-lib",
  "version": "1.0.0",
  "description": "reflow is a JS and SCSS responsive breakpoint library that makes it easy to build UI that flows across any device   🥊  ✨ ",
  "main": "build/index.js",
  "type": "module",
  "scripts": {
      "build": "rm -rf build && rm -rf ./test/build 2>/dev/null || : && tsc && cp -r ./build/. ./test/build",
      "test": "npm run build && npx serve test"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/MeharGaur/reflow.git"
  },
  "keywords": [
    "responsive",
    "breakpoints",
    "mobile",
    "material"
  ],
  "author": "Mehar Gaur",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/MeharGaur/reflow/issues"
  },
  "homepage": "https://github.com/MeharGaur/reflow#readme",
  "devDependencies": {
    "typescript": "^4.4.3"
  }
}
